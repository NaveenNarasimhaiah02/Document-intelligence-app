import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder test — full widget tests require device/emulator context
  // due to Hive and camera dependencies.
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
